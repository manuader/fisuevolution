# Xcode / build
build/
DerivedData/
*.xcodeproj
xcuserdata/
*.xcuserstate
.DS_Store

# SwiftPM
.build/
.swiftpm/

# Asset pipeline (F3)
# Sin barra final: con barra la regla solo matchea directorios, y un `.venv` que
# fuera un symlink se coló al repo apuntando a si mismo (2334083). Al mergearlo,
# git reemplazo el entorno real de quien lo bajara por un enlace circular.
Tools/asset-pipeline/.venv
Tools/asset-pipeline/raw/
Tools/asset-pipeline/cutout/
Tools/asset-pipeline/ComfyUI/
*.safetensors
build-ci/
Tools/asset-pipeline/.secrets/
Tools/asset-pipeline/.chrome-profile/
Tools/asset-pipeline/state/selenium-run.json

# Graphify knowledge graph (generated, ~4MB; rebuild with `graphify update .`)
graphify-out/

# Capturas de QA y worktrees de agentes (no van a la historia)
scratchpad/
.claude/worktrees/

# Workspace de subagent-driven-development (ledger, briefs, reports)
.superpowers/

# Handoffs locales entre agentes (pedido del dueño 2026-08-18): documentación
# de traspaso por máquina, no se versiona.
handoffs/
